/*
 Define a map that associates words with a list of line numbers on which the word might occur.
key: string   value: list<size_t>
*/
#include <iostream>
#include <map>
#include <string>
#include <list>

int main() 
{
    std::map<std::string, std::list<std::size_t>> mListNoLine;
}

